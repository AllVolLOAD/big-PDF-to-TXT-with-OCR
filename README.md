# 📄 big-PDF-to-TXT-with-OCR

Проект для конвертации PDF-файлов в текстовый формат с использованием OCR (Optical Character Recognition). Первая попытка сделать что-то полезное, которая... получилась как всегда 😅

## 🎯 Что делает

Программа позволяет:
- Конвертировать PDF-файлы в текстовый формат с помощью OCR
- Обрабатывать только нужный диапазон страниц (не весь документ целиком)
- Распознавать текст на нескольких языках (русский, английский, французский)
- Работать на Windows и macOS

## 📋 Требования

### Системные зависимости

#### Tesseract OCR
1. **Windows**: Скачайте установщик с [официального репозитория](https://github.com/UB-Mannheim/tesseract/wiki/Downloading-Tesseract-OCR-Engine)
2. **macOS**: Установите через Homebrew:
   ```bash
   brew install tesseract
   ```

#### Poppler (для конвертации PDF в изображения)
1. **Windows**: 
   - Скачайте [poppler-windows](https://github.com/oschwartz10612/poppler-windows/releases/download/v23.01.0-0/Release-23.01.0-0.zip)
   - Распакуйте архив
   - Добавьте папку `bin` в переменную окружения PATH
   
2. **macOS**: Установите через Homebrew:
   ```bash
   brew install poppler
   ```

### Python зависимости

```bash
pip install pytesseract pdf2image pillow
```

## 🚀 Установка

1. Клонируйте репозиторий (или просто скачайте файлы)
2. Установите системные зависимости (Tesseract и Poppler)
3. Установите Python-зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Отредактируйте пути в `main.py` под вашу систему:
   - Путь к Tesseract
   - Путь к Poppler (для Windows)
   - Путь к PDF-файлу
   - Имя выходного файла

## 💻 Использование

1. Запустите скрипт:
   ```bash
   python main.py
   ```

2. Введите параметры:
   - **START page number**: номер начальной страницы
   - **END page number**: номер конечной страницы
   - **LANGUAGE**: язык документа (`rus`, `eng`, или `fra`)

3. Результат будет сохранен в текстовый файл на рабочем столе

## ⚙️ Настройка

В файле `main.py` можно изменить:

```python
ur_path_to_new_file = "name_file.txt"  # Имя выходного файла
PDF_file = Path(r"d_big_100p.pdf")     # Путь к PDF-файлу
```

Пути к Tesseract и Poppler определяются автоматически в зависимости от операционной системы.

## 🐛 Известные проблемы

- Пути к зависимостям жестко прописаны в коде (нужно адаптировать под свою систему)
- Поддержка только Windows и macOS (Linux не поддерживается, хотя легко добавить)
- Ограниченный набор языков (можно расширить)
- Файл PDF должен быть в той же директории или указан полный путь

## 📝 Структура проекта

```
big-PDF-to-TXT-with-OCR/
├── main.py          # Основной скрипт
└── README.md        # Этот файл
```

## 🔧 Как это работает

1. PDF-файл конвертируется в изображения (JPEG) с помощью Poppler
2. Каждое изображение обрабатывается Tesseract OCR
3. Распознанный текст сохраняется в текстовый файл
4. Переносится обработка переносов слов (удаляются `-\n`)

## 📌 Примечания

- Качество распознавания зависит от качества исходного PDF
- Для больших файлов процесс может занять некоторое время
- Рекомендуется обрабатывать документы по частям (диапазонам страниц)

## 🤝 Вклад

Это был первый проект, так что код может быть не идеальным. Если хотите улучшить - welcome! 

## 📄 Лицензия

Используйте на свой страх и риск 😄
